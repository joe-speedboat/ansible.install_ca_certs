---
# vars file for joe-speedboat.install_ca_certs

#
# Defaults for Debian-based Linux systems
#

# Directory where certificates should be placed
ca_certificates_path: /usr/local/share/ca-certificates

# Command to run after adding new certificates
ca_certificates_handler: /usr/sbin/update-ca-certificates
